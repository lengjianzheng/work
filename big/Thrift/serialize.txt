序列化主要用于程序之间的数据传输(也可用于存储)。不同程序使用的语言可能不同，不同语言表示对象的方式也不同。
所以，进行数据交换时，需要一种统一的格式，统一的方法，序列化起的就是这个作用。

常见的有将程序中的对象按照XML格式、JSON格式进行序列化，即转换成相应格式的二进制字节流；然后就可以进行网络传输。接受方按照对应的格式反序列化，得到数据。

Thrift也用到了序列化，不过Thrift是一个RPC服务框架，提供了整套流程，序列化直接嵌套在框架当中，使用时接触不到。

